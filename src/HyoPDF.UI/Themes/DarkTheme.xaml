<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="AccentColor">#4A9FE0</Color>
    <Color x:Key="AccentHoverColor">#2B7CC7</Color>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}" />
    <SolidColorBrush x:Key="SuccessBrush" Color="#2A9B8A" />
    <SolidColorBrush x:Key="ErrorBrush" Color="#5C1A1A" />
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource BgElevatedColor}" />

    <CornerRadius x:Key="RadiusXs">2</CornerRadius>
    <CornerRadius x:Key="RadiusSm">8</CornerRadius>
    <CornerRadius x:Key="RadiusMd">12</CornerRadius>
    <CornerRadius x:Key="RadiusLg">16</CornerRadius>

    <Duration x:Key="AnimationDuration">0:0:0.2</Duration>

    <Color x:Key="TitleBarBgColor">#0A0D12</Color>
    <Color x:Key="TitleBarTextColor">#8896AA</Color>
    <Color x:Key="EmptyStateIconColor">#4A5870</Color>
    <Color x:Key="ToolbarIconColor">#DDE4F0</Color>
    <Color x:Key="SidebarBgColor">#07090C</Color>
    <Color x:Key="ViewerBgColor">#07090C</Color>
    <Color x:Key="BgBaseColor">#07090C</Color>
    <Color x:Key="BgSurfaceColor">#0D1117</Color>
    <Color x:Key="BgElevatedColor">#161C24</Color>
    <Color x:Key="BorderColor">#1E2A3A</Color>
    <Color x:Key="TextPrimaryColor">#EEF2FF</Color>
    <Color x:Key="TextSecondaryColor">#8896AA</Color>

    <SolidColorBrush x:Key="TitleBarTextBrush" Color="{StaticResource TitleBarTextColor}" />
    <SolidColorBrush x:Key="ToolbarIconBrush" Color="{StaticResource ToolbarIconColor}" />
    <SolidColorBrush x:Key="EmptyStateIconBrush" Color="{StaticResource EmptyStateIconColor}" />
    <SolidColorBrush x:Key="TitleBarBgBrush" Color="{StaticResource TitleBarBgColor}" />
    <SolidColorBrush x:Key="SidebarBgBrush" Color="{StaticResource SidebarBgColor}" />
    <SolidColorBrush x:Key="ViewerBgBrush" Color="{StaticResource ViewerBgColor}" />
    <SolidColorBrush x:Key="AccentTintBrush" Color="#334A9FE0" />
    <SolidColorBrush x:Key="BgBaseBrush" Color="{StaticResource BgBaseColor}" />
    <SolidColorBrush x:Key="BgSurfaceBrush" Color="{StaticResource BgSurfaceColor}" />
    <SolidColorBrush x:Key="BgElevatedBrush" Color="{StaticResource BgElevatedColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}" />

    <Style x:Key="HyoContextMenu" TargetType="ContextMenu">
        <Setter Property="Background" Value="#0D1117" />
        <Setter Property="BorderBrush" Value="#1E2A3A" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="12" Opacity="0.4" ShadowDepth="2" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            Effect="{TemplateBinding Effect}">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Resources>
            <Style TargetType="MenuItem">
                <Setter Property="Height" Value="36" />
                <Setter Property="Padding" Value="12,6" />
                <Setter Property="Foreground" Value="#DDE4F0" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="MenuItem">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      ContentSource="Header"
                                                      RecognizesAccessKey="True"
                                                      VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1"
                                               Text="{TemplateBinding InputGestureText}"
                                               Foreground="#808080"
                                               Margin="16,0,0,0"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#161C24" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="#666666" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsHighlighted" Value="True">
                        <Setter Property="Background" Value="#161C24" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="Separator">
                <Setter Property="Background" Value="#1E2A3A" />
                <Setter Property="Height" Value="1" />
                <Setter Property="Margin" Value="4,4" />
            </Style>
        </Style.Resources>
    </Style>

</ResourceDictionary>
