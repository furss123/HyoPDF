<UserControl x:Class="HyoPDF.UI.Views.StatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:HyoPDF.UI.Converters"
             Height="24"
             Background="{DynamicResource BgSurfaceBrush}"
             BorderBrush="{DynamicResource BorderBrush}"
             BorderThickness="0,1,0,0">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis" />
    </UserControl.Resources>

    <Grid Margin="16,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <TextBlock Style="{StaticResource StatusBarText}"
                       Visibility="{Binding Viewer.HasDocument, Converter={StaticResource InverseBoolToVis}}">
                <Run Text="페이지 —" />
            </TextBlock>
            <TextBlock Style="{StaticResource StatusBarText}"
                       Visibility="{Binding Viewer.HasDocument, Converter={StaticResource BoolToVis}}">
                <Run Text="페이지 " />
                <Run Text="{Binding Viewer.CurrentPageNumber, Mode=OneWay}" />
                <Run Text="/" />
                <Run Text="{Binding Viewer.PageCount, Mode=OneWay}" />
            </TextBlock>

            <TextBlock Style="{StaticResource StatusBarSeparator}" />

            <TextBlock Style="{StaticResource StatusBarText}"
                       Visibility="{Binding Viewer.HasDocument, Converter={StaticResource InverseBoolToVis}}">
                <Run Text="확대 —" />
            </TextBlock>
            <TextBlock Style="{StaticResource StatusBarText}"
                       Visibility="{Binding Viewer.HasDocument, Converter={StaticResource BoolToVis}}">
                <Run Text="확대 " />
                <Run Text="{Binding Viewer.ZoomLevel, Mode=OneWay}" />
                <Run Text="%" />
            </TextBlock>

            <TextBlock Style="{StaticResource StatusBarSeparator}" />

            <TextBlock Style="{StaticResource StatusBarText}"
                       Visibility="{Binding Viewer.HasDocument, Converter={StaticResource InverseBoolToVis}}">
                <Run Text="회전 —" />
            </TextBlock>
            <TextBlock Style="{StaticResource StatusBarText}"
                       Visibility="{Binding Viewer.HasDocument, Converter={StaticResource BoolToVis}}">
                <Run Text="회전 " />
                <Run Text="{Binding Viewer.Rotation, Mode=OneWay}" />
                <Run Text="°" />
            </TextBlock>
        </StackPanel>

        <TextBlock Grid.Column="1"
                   Style="{StaticResource StatusBarText}"
                   Text="{Binding ActiveTab.FileName, Mode=OneWay}"
                   TextTrimming="CharacterEllipsis"
                   MaxWidth="400"
                   Visibility="{Binding Viewer.HasDocument, Converter={StaticResource BoolToVis}}" />
    </Grid>
</UserControl>
