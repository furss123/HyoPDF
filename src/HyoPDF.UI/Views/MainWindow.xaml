<Window x:Class="HyoPDF.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:HyoPDF.UI.Views"
        xmlns:conv="clr-namespace:HyoPDF.UI.Converters"
        Title="HyoPDF"
        Width="1280"
        Height="800"
        MinWidth="900"
        MinHeight="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="{DynamicResource BgBaseBrush}">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <views:TitleBarView Grid.Row="0" DataContext="{Binding}" />
        <views:ToolbarView x:Name="Toolbar" Grid.Row="1" DataContext="{Binding}" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <views:SidebarView Grid.Column="0" DataContext="{Binding}" />
            <views:ViewerView Grid.Column="1" DataContext="{Binding}" />
            <views:PageManagerView Grid.Column="2"
                                   DataContext="{Binding}"
                                   Visibility="{Binding Page.IsPanelOpen, Converter={StaticResource BoolToVis}}" />
        </Grid>

        <views:StatusBarView Grid.Row="3" DataContext="{Binding}" />
    </Grid>
</Window>
