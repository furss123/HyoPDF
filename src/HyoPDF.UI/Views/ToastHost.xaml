<UserControl x:Class="HyoPDF.UI.Views.ToastHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:HyoPDF.UI.Converters"
             IsHitTestVisible="False">
    <UserControl.Resources>
        <conv:ToastTypeToBrushConverter x:Key="ToastBrush" />
    </UserControl.Resources>

    <ItemsControl ItemsSource="{Binding Messages}"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Bottom"
                  Margin="0,0,16,16">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Background="{Binding Type, Converter={StaticResource ToastBrush}}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource RadiusMd}"
                        Padding="12,8"
                        Margin="0,0,0,8"
                        MaxWidth="360">
                    <TextBlock Text="{Binding Message}"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               TextWrapping="Wrap" />
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
